<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <title>Location App</title>
</head>
<body>
  <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">Location App</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="collapsibleNavbar">
        <ul class="navbar-nav">
          <li class="nav-item">
            <a class="nav-link" href="/addLocation">Add New Location</a>
          </li>
        </ul>
      </div>
    </div>
  </nav>

  <div class="container">
    <br>
    <div class="text-center"><h2>Location Details</h2></div>
    <br>

    <% if (location) { %>
      <div class="border p-4 rounded">
        <p><b>Name:</b><br> <%= location.location_name %></p>
        <p><b>Image:</b><br> <img src =" /images/<%= location.image %>" width="20%"></p>
        <p><b>Address:</b><br> <%= location.address %></p>
        <p><b>Unit:</b><br> <%= location.unit %></p>
        <p><b>Postal Code:</b><br> <%= location.postal_code %></p>
        <p><b>Country:</b><br> <%= location.country %></p>
        <p><b>Capacity:</b><br> <%= location.capacity %></p>
      </div>
    <% } else { %>
      <p>No location found.</p>
    <% } %>

    <br>
    <a href="/" class="btn btn-secondary">Back</a>
  </div>
</body>
</html>
